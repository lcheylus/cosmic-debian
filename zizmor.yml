rules:
  artipacked:
    ignore:
      - release-cosmic.yml
  superfluous-actions:
    ignore:
      - release-cosmic.yml
      - release-depends.yml
  template-injection:
    ignore:
      - update-apt-repo.yml
